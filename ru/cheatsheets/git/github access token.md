# 🧭 GitHub Personal Access Token (PAT)

> Цифровой ключ, который заменяет пароль при работе с GitHub через Git, API или инструменты.

---

## 🔑 Зачем нужен токен

### 1. Безопасность
GitHub запретил использовать пароль для Git-операций (с августа 2021).  
Пароль = полный доступ ко всему аккаунту.  
Токен можно:
- ограничить по правам,
- установить срок действия,
- отозвать без смены пароля.

> 🛡️ Токен — не мастер-ключ, а временный пропуск с ограничениями.

---

### 2. Автоматизация и интеграции

Нужен для:
- CI/CD систем (GitHub Actions, Jenkins и др.),
- скриптов и ботов,
- вызовов GitHub API.

Пример API-запроса:
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" https://api.github.com/user/repos
```

---

### 3. Работа с Git через HTTPS

При `git clone`, `git push`, `git pull` через HTTPS — Git запрашивает логин и пароль.  
**Пароль = токен.**

Без токена — ошибка 403.

---

### 4. Контроль доступа

Можно создать несколько токенов:
- для чтения,
- для деплоя,
- для редакторов (VS Code, JetBrains),
- для временного доступа.

Если один токен утек — отзови его, остальные работают.

---

## 🧩 Когда нужен токен

| Ситуация | Зачем |
|----------|-------|
| `git push` в приватный репо | Аутентификация |
| GitHub Actions | Клонирование, вызовы API |
| Скрипт создаёт issue | Авторизация |
| VS Code / CLI | Чтобы не вводить данные каждый раз |
| Docker-контейнер | Клонирование приватных репозиториев |

---

## 🆚 Токен vs Пароль

| Характеристика | Пароль | Токен |
|----------------|--------|-------|
| Доступ | Полный | Можно ограничить |
| Отзыв | Менять пароль — всё сломается | Отозвать один токен — остальное работает |
| Git | ❌ Запрещён | ✅ Обязателен |
| API | ❌ Не работает | ✅ Работает |
| Безопасность | Низкая | Высокая |

---

## ✅ Вывод

> Токен нужен, чтобы безопасно работать с GitHub через Git, API или автоматизацию — без риска потерять аккаунт.

---

# 🔐 Как создать токен

### 1. Зайди в GitHub
https://github.com → войди в аккаунт.

---

### 2. Перейди в настройки токенов

Аватар → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**.

> 💡 Пока проще использовать Classic tokens. Fine-grained — для сложных случаев.

---

### 3. Создай новый токен

**Generate new token** → **Generate new token (classic)**.

---

### 4. Настрой параметры

- **Note** — имя, например: `Git-PC`, `CI-Token`.
- **Expiration** — выбери срок (не “No expiration” — лучше 90 дней или 1 год).
- **Scopes** — права:
  - Для Git: `repo` (обязательно),
  - Для Actions: `workflow`,
  - Остальное — по необходимости.

---

### 5. Сгенерируй токен

Нажми **Generate token**.

---

### 6. Скопируй токен

❗ Показывается **только один раз**.  
Сохрани в менеджере паролей: Bitwarden, 1Password, KeePass.

> ⚠️ Не передавай никому. Это твой пароль.

---

# 🛠 Как использовать токен

### В Git (вместо пароля)

```bash
git clone https://github.com/username/repo.git
# Логин: твой GitHub username
# Пароль: токен
```

---

### В URL (не рекомендуется)

```bash
git clone https://<TOKEN>@github.com/<USERNAME>/<REPO>.git
```

> ⚠️ Не используй в публичных репозиториях или скриптах.

---

### Настройка кэширования

Чтобы не вводить каждый раз:

#### Windows / macOS
```bash
git config --global credential.helper manager
```

#### Linux (на 1 час)
```bash
git config --global credential.helper 'cache --timeout=3600'
```

---

# 🕒 Срок действия токена

GitHub требует установить срок — это защита.

---

### Почему срок — это хорошо

- Если токен утёк — через N дней он сам станет бесполезен.
- Соответствует стандартам безопасности.
- Не даёт накапливать старые ключи.

---

### Какие сроки можно выбрать

- **No expiration** — ❗ не рекомендуется.
- **30 дней** — для временных задач.
- **90 дней** — оптимально для большинства случаев.
- **1 год** — максимум для classic-токенов.
- **Custom** — любая дата в пределах года.

---

### Что будет, когда токен истечёт

- `git push` → Authentication failed.
- API → 401 Unauthorized.
- CI/CD → упадёт.
- GitHub пришлёт email за 7 и 1 день до истечения.

---

### Как обновить токен

1. Создай новый токен.
2. Скопируй (только сейчас его видно!).
3. Замени везде: в Git, CI/CD, скриптах.
4. Удали старый токен → **Revoke**.

> 💡 Используй менеджер паролей — он напомнит, когда срок истекает.

---

### Рекомендуемые сроки

| Сценарий | Срок |
|----------|------|
| Личный Git | 90 дней – 1 год |
| CI/CD | 90 дней |
| Временный доступ | 7–30 дней |
| Демо / публичные проекты | 1–7 дней |
| Production | 90 дней + мониторинг |

---

### Советы

- Не используй “No expiration”.
- Давай токенам понятные имена: `Git-Work`, `Deploy-Bot-2025`.
- Регулярно проверяй список токенов → Settings → Developer Settings.
- Удаляй неиспользуемые.

---

# 🔄 Как отозвать токен

1. Settings → Developer settings → Personal access tokens.
2. Найди токен → **Revoke** или **Delete**.

> Отзыв мгновенный. Всё, что использовало токен — перестанет работать.

---

# 💡 Советы

- Используй разные токены для разных задач.
- Храни токены в секретах (GitHub Secrets, .env, Vault) — не в коде.
- Включи 2FA — для некоторых токенов это обязательно.
- Для CI/CD — используй минимальные права.

---

# 🚀 GitHub CLI (`gh`)

Можно не создавать токен вручную:

```bash
gh auth login
```

CLI сам создаст, сохранит и обновит токен — удобно для локальной разработки.

---

# ✅ Итог

> Токен — обязательный инструмент для работы с GitHub.  
> Создавай с осмысленным сроком, отзывай вовремя, не храни в коде — и всё будет работать безопасно.

---

Нужны PDF, чек-лист или скрипт напоминаний — пиши. Сделаю без пафоса. Только полезное.